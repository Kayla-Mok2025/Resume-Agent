# 复制此文件为 .env 并填写你的 Dify API Key
DIFY_API_KEY=your_dify_api_key_here
DIFY_BASE_URL=https://api.dify.ai/v1
PORT=3001

# Dify 应用类型：
#   chatflow → Chatbot / Chatflow 类型，调用 /chat-messages（默认）
#   workflow → Workflow 类型，调用 /workflows/run
DIFY_APP_MODE=chatflow
